[project]
name = "autoresearch-kit"
version = "0.1.0"
description = "Onboarding wizard + visual dashboard for Karpathy's autoresearch"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
